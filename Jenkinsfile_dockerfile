pipeline {
    agent { 
    dockerfile {
        filename 'Dockerfile'
        label 'my-defined-label'
        args '-v /tmp:/tmp'
    } }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
               
            }
        }
    }
}
